.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
dist/
build/
*.sqlite3
# Exception: the demo vault is synthetic data from a real live GPT-5.6 run,
# committed so judges can inspect genuine receipts without an API key.
# Never commit a real engagement vault - it holds raw documents and mappings.
!demo/demo-vault.sqlite3
.DS_Store
demo/last_seed.json
.env
.openai_session_id

# Author-only / submission drafts — not for the public judge-facing tree
HANDOFF.md
SUBMISSION.md
SECURITY-REVIEW.md
VIDEO-SCRIPT.md
screenshots/
docs/superpowers/prompts/
docs/superpowers/plans/
tools/devpost-gallery/LEAVE-AND-RUN.md
tools/devpost-gallery/fixtures/engagement_notes.txt

# Demo / recording outputs — YouTube carries the video; keep PNGs in docs/media/
docs/media/**/*.mp4
docs/media/**/*.webm
docs/media/.chatgpt-*.png
docs/media/.pdf-lifecycle-tmp/
*.mp4
*.webm

# Local ChatGPT login profile for the recorder (cookies) — never commit
tools/devpost-gallery/node_modules/
tools/devpost-gallery/.chrome-profile/
